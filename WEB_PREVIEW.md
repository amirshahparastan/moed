# Web Preview

برای بررسی UI/UX و منطق قبل از Android:

```bash
flutter pub get
flutter run -d chrome
```

Web از همان صفحات و state اپ اصلی استفاده می‌کند. ذخیره‌سازی Preview در مرورگر انجام می‌شود؛ قابلیت‌های سیستم‌عامل مثل Notification و biometric در Android واقعی تست می‌شوند.
